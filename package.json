{
    "name": "secure-chat",
    "version": "0.0.1",
    "scripts": {
        "start-client": "cd frontend && node scripts/start.js",
        "build": "(cd frontend && node scripts/build.js) && (cd ../backend && yarn install)",
        "test": "cd frontend && node scripts/test.js",
        "start": "cd backend && npm start"
    },
    "eslintConfig": {
        "extends": "react-app"
    },
    "browserslist": {
        "production": [
            ">0.2%",
            "not dead",
            "not op_mini all"
        ],
        "development": [
            "last 1 chrome version",
            "last 1 firefox version",
            "last 1 safari version"
        ]
    }
}